{
  "name": "quick-fresh-app",
  "version": "1.0.0",
  "description": "Quick & Fresh - Product List & Contact Records manager",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "setup": "npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "multer": "^2.0.0",
    "uuid": "^9.0.1"
  }
}

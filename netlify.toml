[build]
  base = "client"
  command = "npm run build"
  publish = "client/dist"

# React Router-style SPA fallback: κάθε path επιστρέφει το index.html
# ώστε client-side navigation να μη σκάει σε 404 μετά από refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
